import typePrefix from 'state/helpers/type-prefix'

const prefix = typePrefix('ui')

export const INITIALIZE = prefix('INITIALIZE')
export const MERGE = prefix('MERGE')
export const REPLACE = prefix('REPLACE')
