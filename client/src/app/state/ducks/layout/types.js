import typePrefix from 'state/helpers/type-prefix'

const prefix = typePrefix('layout')

export const CLOSE = prefix('CLOSE')
export const OPEN = prefix('OPEN')

export const UPDATE_MENU_WIDTH = prefix('UPDATE_MENU_WIDTH')
export const UPDATE_STILL_HEIGHT = prefix('UPDATE_STILL_HEIGHT')
