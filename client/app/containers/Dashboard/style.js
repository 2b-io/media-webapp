import prefix from 'helpers/vendor-prefix'
import { columnLayout } from 'styles/layout'

export default prefix({
  wrapper: {
    display: 'flex',
    flexWrap: 'wrap'
  },
  widget: {
    width: '100%',
    marginLeft: '15px',
    marginRight: '15px',
    marginBottom: '15px'
  }
})
